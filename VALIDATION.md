# 0.2.0 本地验证记录

日期：2026-09-12。仓库从 `https://github.com/qhqj/nekox-oss-tool.git` 克隆，起点提交 `8ca2a91`。此记录覆盖本地构建与验证，不代表 GitHub Release 发布记录。

## 已实现

- 多账号列表、名称和备注、独立配置、新增/保存/连接/本机移除；连接失败保持原账号。
- 当前连接与尚未重新连接的已保存修改分开显示；上传标题显示实际 Bucket。
- 多文件选择和文件夹递归，保留根文件夹、子目录与相对路径。
- 同名覆盖、不上传、自动重命名，可应用到本轮后续冲突。
- 上传队列进度、失败重试、待上传项移除、完成当前文件后停止；逐项显示最终 Key 与 URL。
- Windows 当前用户 DPAPI 加密账号库，旧配置迁移；Web Secret / STS Token 仅保留在会话。

## 自动化验证

| 检查 | 结果与范围 |
| --- | --- |
| `npm run typecheck` | 通过 |
| `npm test` | 6 个测试文件，75 项通过 |
| `npm run build` | 通过；ali-oss 引入的主分块约 830 KB，仍有 Vite 大分块提示 |
| 原生账号库测试 | 3 项通过：DPAPI 往返和损坏拒绝、保存替换与无效保存保留旧库、格式和大小限制 |
| `git diff --check` | 通过 |
| `npm audit --omit=dev` | 检查时报告 0 项漏洞 |

前端测试覆盖：账号切换失败、存储失败、本机账号删除、上传期间禁止切换、已保存修改待重连标记、分页重复点击保护、账号草稿和密码框、旧配置迁移与凭证剥离、非法路径、分页同名检测、覆盖/跳过/改名、并发冲突错误、失败重试和停止队列。SDK 错误测试使用合成敏感标记，验证提示不回显请求内容。

原生测试只使用合成凭证，验证加密文件中不存在明文测试 Secret、密文篡改/截断无法解密、无效写入保留旧数据。

## 界面检查

- 在本地 Vite 页面操作了账号资料保存与本机移除，演示账号无真实凭证，检查后已清理。
- 使用真实浏览器文件夹选择器读取项目内的测试目录，确认根文件夹和子目录都进入上传队列。
- 在隔离的本地模拟页面检查同名弹窗、“应用到后续”与改名结果；模拟服务不会请求 OSS。
- 检查了上传队列、三种冲突处理按钮及结果排版，包括 960 × 640 窗口下的滚动和底部操作按钮。

## 验证边界

- 未使用真实 AccessKey 或 STS Token，未读写真实 OSS Bucket；真实 CORS、RAM 权限、大文件网络稳定性需用自己的测试 Bucket 验收。
- 同名保护请求头在 OSS 已开启或暂停版本控制的 Bucket 中会被忽略，并发写入仍有限制，详见 README 中的官方说明。
- 空文件夹不生成对象；停止队列会等待当前文件完成；关闭弹窗后队列不持久化。
- 安装包未签名。构建、启动检查与实际安装/升级验收分别记录，不将前者视为后者。

## Windows 安装包

- `scripts/package-desktop.ps1` 成功生成 Windows x64 NSIS 安装包；最终构建使用已通过上述 75 项前端测试的代码。
- 文件：`install/Nekox-OSS-Tool-0.2.0-windows-x64-setup.exe`，2,936,609 字节。
- SHA-256：`b41d5e403aafd74fc987f0ac82f0dc69cdbee580bf8610ac15b893f62385eb61`，已重新计算并与 `install/SHA256SUMS.txt` 比较一致。
- 当前版本号在 npm、Cargo 与 Tauri 配置中统一为 `0.2.0`。
- 原生启动检查通过：使用项目内隔离的 WebView2 用户目录启动生成的程序，10 秒后进程仍在运行且响应，窗口标题为 `Nekox OSS Tool`；正常关闭后退出码为 0。未读取或创建账号库，未执行安装程序。
- 可执行文件 ProductVersion / FileVersion 均为 `0.2.0`。本次未做安装向导、覆盖升级或真实账号联调。
